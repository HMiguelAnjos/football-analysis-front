window.__APP_CONFIG__ = {
  // URL da API de futebol (preferida). No deploy, setar FOOTBALL_API_URL no container.
  VITE_FOOTBALL_API_URL: "$FOOTBALL_API_URL",
  // Compat com setup single-backend antigo (fallback).
  VITE_API_URL: "$API_URL",
};
